syntax = "proto3";

package v1;

option go_package = "github.com/dwipcs/auth/v1";

enum AuthError {
  AUTH_ERROR_UNSPECIFIED = 0;
  AUTH_ERROR_INVALID_CREDENTIALS = 1;
  AUTH_ERROR_USER_NOT_FOUND = 2;
  AUTH_ERROR_UNAUTHORIZED = 3;
}
